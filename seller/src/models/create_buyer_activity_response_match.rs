/*
 * Crossly Seller API
 *
 * The seller-facing Crossly API — listings, crossposting, orders, analytics. Authenticate with a Personal Access Token (`Authorization: Bearer crossly_pat_…`) or a seller OAuth token (`crossly_oat_…`), scoped `<domain>:<read|write>`. Buyer endpoints are a SEPARATE API with a separate principal — see the Crossly Buyer API.
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 * Generated by: https://openapi-generator.tech
 */




#[derive(Clone, Debug, PartialEq, Default, Serialize, Deserialize)]
pub struct CreateBuyerActivityResponseMatch {
    #[serde(rename = "verdict")]
    pub verdict: Verdict,
    #[serde(rename = "offer")]
    pub offer: Box<crate::models::CreateBuyerActivityResponseMatchOffer>,
    /// Positive when cheaper. Null when no page price was supplied.
    #[serde(rename = "savingCents", skip_serializing_if = "Option::is_none")]
    pub saving_cents: Option<f32>,
    /// TRUE means the offer's condition and the page's are comparable. False means we matched the item but not its state — a used Crossly copy against a retailer's new one — and the UI must say so rather than claim a saving.
    #[serde(rename = "conditionComparable")]
    pub condition_comparable: bool,
    /// Always false today. Crossly shipping is computed at checkout from the buyer's address, which Scout does not have and should not send. Present so the surface that renders \"before shipping\" is reading a fact rather than hard-coding an assumption that stops being true when we add it.
    #[serde(rename = "shippingKnown")]
    pub shipping_known: bool,
    #[serde(rename = "catalog", skip_serializing_if = "Option::is_none")]
    pub catalog: Option<Box<crate::models::GetCatalogLookupResponseCatalog>>,
    /// Other buyable offers for the SAME item, cheapest first, best excluded.  Deliberately not \"you might also like\". We have no behavioural data to build that from, and inventing it would put unrelated items under a badge whose entire value is that it only ever appears when we have the thing the buyer is actually looking at.  What these ARE is the same product from other sellers, in other conditions, at other prices — which is the choice a buyer on a product page genuinely wants, and the one a single \"cheapest\" result hides.
    #[serde(rename = "alternates")]
    pub alternates: Vec<crate::models::CreateBuyerActivityResponseMatchOffer>,
}

impl CreateBuyerActivityResponseMatch {
    pub fn new(verdict: Verdict, offer: crate::models::CreateBuyerActivityResponseMatchOffer, condition_comparable: bool, shipping_known: bool, alternates: Vec<crate::models::CreateBuyerActivityResponseMatchOffer>) -> CreateBuyerActivityResponseMatch {
        CreateBuyerActivityResponseMatch {
            verdict,
            offer: Box::new(offer),
            saving_cents: None,
            condition_comparable,
            shipping_known,
            catalog: None,
            alternates,
        }
    }
}

/// 
#[derive(Clone, Copy, Debug, Eq, PartialEq, Ord, PartialOrd, Hash, Serialize, Deserialize)]
pub enum Verdict {
    #[serde(rename = "cheaper")]
    Cheaper,
    #[serde(rename = "not_cheaper")]
    NotCheaper,
    #[serde(rename = "no_page_price")]
    NoPagePrice,
    #[serde(rename = "currency_mismatch")]
    CurrencyMismatch,
}

impl Default for Verdict {
    fn default() -> Verdict {
        Self::Cheaper
    }
}

