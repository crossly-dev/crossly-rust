/*
 * Crossly Buyer API
 *
 * The buyer-facing Crossly API — cart, orders, wishlists, offers, cashback. A DIFFERENT principal from the seller API: authenticated by a buyer OAuth token scoped `buyer:*` and resolved by `resolveBuyer`. A seller Personal Access Token will NOT authenticate these endpoints.
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 * Generated by: https://openapi-generator.tech
 */

/// Pagination : Present only when the endpoint pages. Absent — not null — when it returns everything.



#[derive(Clone, Debug, PartialEq, Default, Serialize, Deserialize)]
pub struct Pagination {
    /// 1-indexed page number.
    #[serde(rename = "page")]
    pub page: i32,
    /// Rows per page.
    #[serde(rename = "limit")]
    pub limit: i32,
    /// Total matching rows, when the endpoint counts them.
    #[serde(rename = "total", skip_serializing_if = "Option::is_none")]
    pub total: Option<i32>,
    /// Derived from total and limit.
    #[serde(rename = "totalPages", skip_serializing_if = "Option::is_none")]
    pub total_pages: Option<i32>,
}

impl Pagination {
    /// Present only when the endpoint pages. Absent — not null — when it returns everything.
    pub fn new(page: i32, limit: i32) -> Pagination {
        Pagination {
            page,
            limit,
            total: None,
            total_pages: None,
        }
    }
}


